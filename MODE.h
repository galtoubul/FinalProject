#ifndef SUDOKU_MODE_H
#define SUDOKU_MODE_H

/**
 * MODE Summary:
 * This interface contains only enum type that used in the program
 */
typedef enum {
    INITMODE,
    SOLVEMODE,
    EDITMODE
} MODE;


#endif
